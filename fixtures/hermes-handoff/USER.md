# Fixture operator

The fixture operator prefers Korean first, with English when a plugin UI is English-only.

## Standing rules
- Morning brief uses unread mail and today's calendar only.
- Drafts are fine. Sending mail or confirming events always waits for a human.
- Do not store API keys or tokens.
